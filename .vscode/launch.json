{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run minecraft_clone",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/minecraft_clone",
      "cwd": "${workspaceFolder}/build",
      "preLaunchTask": "Build minecraft_clone"
    },
    {
      "name": "Run minecraft_clone (no build)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/minecraft_clone",
      "cwd": "${workspaceFolder}/build"
    }
  ]
}
